stages:
  - release

publish:
  stage: release
  script:
    - npm config set -- '//registry.npmjs.org/:_authToken' "${NPM_TOKEN}"
    - npm profile get
    - echo ready to publish package to npm registry
  #  - npm publish #1
  only:
    - web
    - master
  when: manual
    